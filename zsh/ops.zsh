export PATH="$PATH:$HOME/arcanist/arcanist/bin"

export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && emulate sh -c 'source "$NVM_DIR/nvm.sh"'  # This loads nvm
[ -s "$NVM_DIR/bash_completion" ] && emulate sh -c 'source "$NVM_DIR/bash_completion"'  # This loads nvm bash_completion
